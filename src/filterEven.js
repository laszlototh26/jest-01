
function filterEven (numbers) {

  if ( !Array.isArray(number)) throw new Error ('Argument must be an array');

  
  const evenNumbers = numbers.filter ((item, index) => {



    
  }

    const [, remainder] = divideNums(item, 2);




  
  
  
  )
  
  
  

}
  module.exports = {
    filterEven
  }
